apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deusops-may.fullname" . }}-test-connection"
  labels:
    {{- include "deusops-may.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deusops-may.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
